# Netlify 배포 가이드 — 포항세화고등학교

## 왜 열리지 않았습니까

Netlify는 배포 폴더 최상단에서 `index.html`을 찾아 첫 화면으로 보여줍니다.
그런데 앞서 드린 `포항세화고_웹배포패키지_v1.2.zip`은 **Next.js 앱**입니다.
Next.js는 `index.html` 파일이 아예 존재하지 않고, `npm run build`를 실행해야
그때 비로소 `.next` 폴더 안에 화면이 만들어지는 구조입니다.

즉 압축을 풀어 그대로 올리셨다면 Netlify 입장에서는
**보여줄 첫 화면이 없는 폴더**였고, 그래서 "Page not found"가 뜬 것입니다.
파일이 잘못된 것이 아니라 올리는 방식이 맞지 않았습니다.

해결 방법은 두 가지입니다.

---

## 방법 1 · 데모 사이트 (5분, 가장 쉬움)

`netlify-demo` 폴더를 쓰십시오. `index.html`이 들어 있어 빌드 없이 바로 열립니다.

1. https://app.netlify.com/drop 접속 (로그인해도 되고 안 해도 됩니다)
2. **`netlify-demo` 폴더를 브라우저 창에 통째로 끌어다 놓기**
3. 끝입니다. `https://무작위이름.netlify.app` 주소가 나옵니다
4. 이름 변경은 Site configuration → Change site name

### 주의: 폴더를 올리십시오, ZIP 안의 파일을 하나씩 올리지 마십시오
`index.html`과 `data.js`가 **같은 위치**에 있어야 합니다.
`data.js`가 빠지면 화면은 뜨지만 학생 목록이 비어 있습니다.

### 접속해서 시험해 볼 것
로그인 화면 오른쪽에 데모 계정 6개가 있습니다. 눌러보시면 됩니다.

| 계정 | 확인할 내용 |
|---|---|
| 학부모 · 자녀 2명 | 자녀 선택 화면이 먼저 뜨는지 |
| 학부모 · 자녀 1명 | 바로 성적 화면으로 가는지 |
| 학생 본인 | 내신·모의고사 추이 차트 |
| 담임 교사 | 학급 현황 탭이 추가로 보이는지 |
| 관리자 | 전교생 100명이 잡히는지 |
| 010-0000-0000 | **접속이 거부되어야 정상입니다** |

---

## 방법 2 · 실제 앱 (30분, 로그인·DB 동작)

방법 1은 화면만 보여주는 데모입니다. 실제 인증과 DB가 필요하면 이쪽입니다.

### 준비
```bash
npm install -D @netlify/plugin-nextjs
```
`nextjs-netlify/netlify.toml`을 **저장소 최상단**(package.json 옆)에 복사합니다.
이 플러그인이 없으면 화면은 떠도 **API가 전부 404**가 됩니다.

### 절차
1. GitHub에 Private 저장소로 올립니다 (앞서 드린 GitHub 배포킷 사용)
2. Netlify → Add new site → Import an existing project → GitHub 선택
3. 빌드 설정은 `netlify.toml`이 자동 적용하므로 손대지 마십시오
4. Site configuration → Environment variables에 아래를 넣습니다

| 변수 | 값 |
|---|---|
| `DATABASE_URL` | **반드시 `app_rw` 계정**. 관리자 계정 금지 |
| `SESSION_SECRET` | `openssl rand -base64 48` 결과 |
| `PHONE_HASH_PEPPER` | `openssl rand -base64 32` 결과 |
| `PHONE_ENC_KEY` | `openssl rand -base64 32` 결과 |
| `SMS_PROVIDER` | 시연 중에는 `console` |
| `SCHOOL_NAME` | 포항세화고등학교 |

5. Deploy 후 Functions 로그에서 인증번호를 확인합니다

### DB 연결에 관한 주의
Netlify Functions는 요청마다 새로 뜨고 사라집니다.
일반 PostgreSQL에 직접 붙이면 **연결 수가 금방 고갈됩니다.**
Neon·Supabase처럼 **연결 풀러(PgBouncer)가 있는 DB**를 쓰시고,
연결 문자열은 반드시 **pooled 주소**(보통 `-pooler`가 붙어 있습니다)를 넣으십시오.

RLS 세션변수는 `set_config(..., true)`로 트랜잭션 범위에서만 설정해야
풀러 환경에서 다른 요청에 값이 새지 않습니다. 코드는 이미 그렇게 되어 있습니다.

---

## 어느 쪽을 고르시겠습니까

| | 방법 1 데모 | 방법 2 실제 앱 |
|---|---|---|
| 소요 시간 | 5분 | 30분 |
| 빌드 | 없음 | npm run build |
| DB | 불필요 | 필요 |
| 로그인 | 흉내만 냄 | 실제 SMS 인증 |
| 권한 차단 | 화면 수준 | DB(RLS) 수준 |
| 실제 성적 | **절대 금지** | 절차 완료 후 가능 |
| 용도 | 학교 시연·의사결정 | 운영 |

학교에 보여드리고 승인을 받는 것이 목적이라면 **방법 1로 충분합니다.**

---

## 반드시 지키실 3가지

**하나. 실제 학생 정보를 넣지 마십시오.**
방법 1의 `data.js`는 주소만 알면 누구나 내려받아 열어볼 수 있습니다.
암호화도 인증도 없습니다. 가상 데이터 전용입니다.

**둘. Netlify 서버는 국외에 있습니다.**
실제 성적을 올리는 순간 개인정보 국외이전에 해당합니다.
동의서·처리위탁 계약·경상북도교육청 협의를 마치기 전에는
국내 서버 또는 교내 서버로 옮겨 운영하셔야 합니다.

**셋. `DATABASE_URL`에 관리자 계정을 넣지 마십시오.**
RLS가 통째로 무력화되어 누구나 전교생 성적을 보게 됩니다.
반드시 `app_rw` 계정입니다.

---

생성일 2026-08-07
